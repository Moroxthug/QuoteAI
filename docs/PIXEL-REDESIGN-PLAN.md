# QuoteAI pixel-accurate redesign — plan v2 (supersedes the Phase 30-37 approach)

**Status: planning only, nothing in this doc is built yet.**

## Why this doc exists

Phases 30-37 (see `docs/DESIGN-SYSTEM.md`) treated the two user-supplied mockups as a *palette and radius* reference: navy/Figtree/pill tokens were layered onto the existing shadcn + Tailwind component set, and pages got a mechanical `rounded-xl` → `rounded-[var(--radius)]` sweep. That was a misread of the ask.

The mockups (now saved permanently at `docs/mockups/homepage-mockup.html` and `docs/mockups/dashboard-mockup.html` — previously they only existed as pasted chat content and were lost between conversations, which is why Phase 33 had to work from a written description) are **complete, self-contained design systems**: real CSS custom classes (`.card`, `.chip-*`, `.tgl`, `.seg`, `.kan-col`, `.pill`, `.sb-link`, `.bubble`, `.tbl`, `.stat-card`, `.form-grid`/`.field`, `.dropzone`, `.mega-card`, `.tile`, etc.), exact spacing/shadow/radius values, and exact page structure. The instruction is to make the real app **visually identical** to these two files — same DOM shape, same classes, same spacing — with QuoteAI's real data and backend wired in, and to treat these two files as the component library for anything new going forward. Logo is the one deliberate exception (keep the current one).

## Two decisions locked in for this rebuild (confirmed with user 2026-09-16)

1. **No dark mode.** Both mockups are light-only. Dark mode (ThemeProvider, `theme-toggle.tsx`, every `dark:` Tailwind variant) is being removed, not extended.
2. **Verbatim CSS, not a Tailwind translation.** The mockups' own `:root` tokens and component classes get ported into the app as real CSS (new stylesheet, imported once) and pages are rebuilt to use those exact classes. Tailwind stays only for layout utilities (flex/grid/gap) where the mockup doesn't define its own layout class. Existing shadcn primitives (`button.tsx`, `badge.tsx`, `card.tsx`, etc.) are superseded wherever the mockup has a direct equivalent — this is a deliberate move away from "shadcn + tokens" toward "the mockup's own system."

## Source-of-truth files

- `docs/mockups/homepage-mockup-v2.html` — **current homepage source of truth as of Phase 48e (2026-09-17), supersedes `homepage-mockup.html` below.** Richer marketing site: announcement bar, mega-menu header (Products mega / Trades / WhatsApp / Guides), dark hero with a `.doc-mock` receipt visual, 6-tile product grid + "also included" chip row, 3 alternating story splits, an embedded WhatsApp section with 3 numbered steps, a `.cmp` comparison table (quoteai vs Jobber/Housecall Pro vs Excel & Word), navy impact-stats band, 3-card newsroom, `.rev-card` reviews, a guides grid, a `.marquee` trades ticker, a `.dd-feat`/`.dd-grid` deep-dive essay section, an image-bg trial CTA, and a 6-column footer. **When this file and `homepage-mockup.html` disagree, this file wins for the homepage.**
- `docs/mockups/homepage-mockup.html` — the original (v1) homepage mockup Phases 38-48d were built from. Kept for history; no longer the active source of truth for the homepage, but its component vocabulary (`.tile`, `.split`, `.news-card`, etc.) mostly carried forward into v2 unchanged.
- `docs/mockups/dashboard-mockup.html` — full app shell + every dashboard view as a single-page mock with view-switching JS (sidebar with 4 grouped sections + bottom notifications/user, topbar with search/support/bell/avatar, and markup for: dashboard home, quotes, clients, leads (kanban), contracts, jobs, team, assistant (chat), analytics, price list, invoices, archive, import, settings (stacked cards, no tabs), notifications (full page)). Still the source of truth for the dashboard — unaffected by the homepage's v2 mockup.

Read the relevant mockup file directly (not this doc's prose) before touching a page in any phase — the exact class names and structure live there.

## Gaps the mockup exposes that the current app doesn't have

- **Settings is currently tabbed** (`settings.tsx` + `settings-business-tab.tsx` + `settings-security-tab.tsx`). The mockup's settings view is one continuous stack of cards (Business profile / Branding / Quote defaults / Notifications / Billing & plan / Security), no tabs at all. **Decided in Phase 45: kept as tabs, not flattened.** The real settings page covers Account, Business, Billing, WhatsApp, Widget, Usage, Integrations (Stripe Connect, Financeit, QuickBooks, Wave, Flinks, Meta Lead Ads, Google LSA, Calendar, Email, Developer/webhooks), and Security — genuinely more surface than the mockup's 6-card stack, with real third-party integrations that have no mockup equivalent. Flattening would either lose real functionality or produce an unreasonably long single page. Instead the tab switcher itself and every tab's content were reskinned to the mockup's own vocabulary (`.pills`/`.pill` for the switcher — same pattern as `team.tsx`'s tabs; `.card`/`.card-head`/`.card-foot`, `.form-grid`/`.field`, `.set-row`+`MockupToggle` wrapping `.tgl`/`.tgl.on`, `.logo-drop`/`.logo-tile`, `.chip-*`, `.cta-link` for the rest), so it now reads as the same design system even though its structure diverges from the mockup's flat stack. `billing.tsx` (`/dashboard/billing`) and `profile.tsx` (`/dashboard/profile`) are separate standalone routes — not duplicates of the Billing/Account tabs inside Settings — `billing.tsx` is the lightweight upgrade/usage destination linked from paywall CTAs across the app (assistant panel, analytics, invoices, dashboard quick actions), while Settings' Billing tab is the fuller billing-management surface reached from the account-menu "Plan & billing" link. Both were reskinned; neither was removed.
- **Archive and a full-page Notifications view don't exist yet** (`App.tsx` has no route for either; notifications today is only the topbar bell popover). Building these as more than static shells means real backend concepts (a soft-delete/archive table, a notifications feed). Phase 47 flags this explicitly rather than quietly building fake UI.
- **Documents, Profile, Onboarding, and the Billing page have no literal mockup section** — apply the shared vocabulary (`.card`, `.form-grid`/`.field`, `.dropzone`, `.chip`, `.set-row`/`.tgl`) consistently even without a 1:1 template, per the "use the elements in these two files" instruction.

## Phase plan (each phase its own conversation, per established convention; numbering continues from 37)

- **Phase 38 — Foundation.** Port both mockups' `:root` tokens (union of both files' variables) and base/global classes (`.btn*`, `.chip*`, `.cta-link`, typography resets) into a new stylesheet, imported app-wide. Strip dark mode (ThemeProvider, `theme-toggle.tsx`, `dark:` variants — flag any large/risky removals rather than doing a blind mechanical strip). Establish the interop rule (mockup classes for componentry, Tailwind for layout only) so every later phase follows the same convention.
- **Phase 39 — Dashboard shell.** Rebuild `dashboard-layout.tsx` to literally match `.sidebar`/`.sb-*` (rail-collapse animation, grouped nav, PRO badges wired to real plan gating, bottom notifications+user block) and `.topbar`/`.search`/`.bell*`/`.pop` (popover, avatar initials), including the mobile scrim+slide-in behavior.
- **Phase 40 — Dashboard home.** `dashboard/index.tsx`: page-head + sliding-thumb `.seg` period control, `.composer` AI input card, `.stat-grid`, `.mid-grid` (recent quotes list + revenue bar chart + follow-ups card), `.qa-grid` quick actions — all wired to real data/mutations, structure ported verbatim.
- **Phase 41 — Quotes, Clients, Leads.** `.card`+`.toolbar`/`.pills`+`.tbl` for quotes/clients; leads kanban (already structurally a kanban since Phase 34) re-skinned to `.kanban`/`.kan-col`/`.kan-card`.
- **Phase 42 — Contracts, Jobs, Team.** Pill-filtered `.tbl`s, `.pbar` progress cells, role `.chip-*`s.
- **Phase 43 — Invoices, Analytics, Price list (catalog).** Stat-grid + tall bar chart + `.hbar` breakdown + top-clients table; catalog table with margin chip.
- **Phase 44 — Assistant.** `.chat-grid` (thread list + `.bubble` chat + suggestion `.pill`s + `.chat-in`), wired to the real assistant backend.
- **Phase 45 — Settings, Billing, Team permissions, Profile.** Resolve the tabs-vs-stack gap noted above; port `.form-grid`/`.field`/`.set-row`/`.tgl`/`.logo-drop` for the rest.
- **Phase 46 — Documents, Onboarding, Imports. DONE.** Import page (`pages/dashboard/imports/index.tsx`) rebuilt with the literal `.steps-3` + `.src-grid` + `.dropzone` markup from the mockup (a real 2-source picker — spreadsheet vs. PDF, matching what the backend actually supports, not the mockup's fictional QuickBooks/Xero); `.steps-3`/`.step-card`/`.step-num`/`.src-grid`/`.src`/`.dropzone` classes ported into `mockup-system.css`. Documents (`pages/dashboard/documents.tsx`) and Onboarding (`pages/onboarding.tsx`) reskinned with the existing shared vocabulary (`.card`/`.card-head`/`.card-foot`, `.dropzone`, `.set-row`, `.chip-*`, `.form-grid`/`.field`, `.logo-drop`/`.logo-tile`) by extension, no literal mockup section for either. New i18n keys added (EN+FR) for the import steps/source copy.
- **Phase 47 — Notifications page & Archive, real backend. DONE (2026-09-17).**
  - **Notifications page**: `pages/dashboard/notifications.tsx`, reuses the existing `GET /api/notifications` / `POST /api/notifications/read`. `.n-row`/`.n-dot` markup ported into `mockup-system.css`. The sidebar bell link now navigates to `/dashboard/notifications` instead of opening the popover (topbar bell keeps the popover).
  - **Archive**: added nullable `archivedAt`/`archivedByName` to quotes, clients, invoices, contracts and projects (migration `0032_phase47_archive.sql`, applied). Archive/restore endpoints added per entity (`POST /:id/archive`, `POST /:id/restore`), all 5 entities' list endpoints now filter out archived rows by default, and a unified `GET /api/archive` (new `routes/archive.ts`) unions all 5 sorted by `archivedAt desc`. `pages/dashboard/archive.tsx` renders it with a Restore action. Real "Archive" action wired into: quotes list row menu, invoice detail (paid/void only), job detail (completed only), contract detail (closed only). **Clients are the one exception**: the dashboard Clients page is still a virtual list derived from `quotes.client_data` (not `clientsTable`, confirmed via `routes/clients.ts`), so there's no real client row to archive from the UI — the `archivedAt` column exists on `clientsTable` for symmetry but nothing sets it; the Archive page silently supports a "client" item type for if/when that page gets wired to `clientsTable` for real.
  - Renamed the Documents nav label from "Archive" (leftover branding, collided with the new page) to "Documents".
- **Phase 48 — Homepage. DONE (2026-09-17).** `public-layout.tsx` + `home.tsx` rebuilt around the mockup's own vocabulary. **Scope decision (confirmed with user):** restyle everything, drop nothing — the homepage has substantially more real content than the mockup mocks up (pricing, comparison table, demo video, SEO deep-dive essay, sectors/cities accordion, guides grid), all left in place and reskinned rather than deleted for SEO/conversion value.
  - `public-layout.tsx`: added `.annc` announcement bar (dismissible, sessionStorage), rebuilt the header as `.site-head`/`.hd-in`/`.main-nav`/`.hd-right` with a new real "Our products" `.mega` (4 tiles: AI Quotes/Smart CRM/Invoicing & Payments/Integrations, linking to homepage anchors + `/whatsapp/`) alongside the existing real Trades mega (kept, restyled). Mobile drawer rebuilt with `.m-menu`/`.m-link`/`.m-label`. Footer rebuilt as `.footer.on-dark`/`.ft-grid` (5 columns: brand+tagline, Products, Trades, Guides, Company) with `.ft-fine`/`.ft-bottom`/`.locale`; no socials row added since no real social profile URLs exist anywhere in the codebase (not fabricated). `LanguageToggle` gained a `variant="dark"` for the footer's `.locale` button.
  - `home.tsx`: added a `.hero-kicker` line to the existing hero (kept as-is otherwise — already exceeds the mockup's plain dark hero in richness, so it wasn't forced onto a navy background). Added a new `.products`/`.tiles` 4-tile section after the hero. Consolidated the old WhatsApp teaser banner + "how it works" WhatsApp demo + quote-PDF demo + job-site-CRM demo into one `.stories` container with 4 alternating `.split`s (`#story-quotes`, `#story-crm`, `#story-invoicing` — this one newly written using a small real invoice-status mockup since no invoice visual existed before, `#story-whatsapp` reusing the existing `WhatsAppChatDemo`). Added a real `.newsroom` section pulling the 3 most recent `BLOG_ARTICLES`. Closing CTA rebuilt as `.trial`/`.trial-in` band (same mesh-blob dark background, no stock photo).
  - `stats-bar.tsx`: `StatsBar` repurposed as the mockup's dark `.impact`/`.stat-grid` band (moved from directly under the hero to after the story splits, matching mockup order) — still the same real, live-counted numbers (trades count, cities count, rating), nothing invented.
  - `.stat-grid` collision: the dashboard (Phase 40) already uses that class name for its 4-stat-card grid. Scoped the homepage's version under `.impact .stat-grid` so the two don't clash.
  - Verified live in the Browser pane (homepage needs no auth): header megas, mobile drawer, product tiles, story splits, impact counters, newsroom cards, trial CTA, footer, and FR language switch all checked at desktop + mobile widths.
- **Phase 48b — Homepage locked to the mockup literally. DONE (2026-09-17).** User reviewed the shipped Phase 48 homepage and asked for it to be rebuilt to match the mockup **exactly**, reversing the "restyle everything, keep everything" call — this exact structure is now the locked design system for future SEO-page work, not just a style reference.
  - **Confirmed with user:** impact band keeps real numbers (not the mockup's invented 42,000+ teams / $4.2B), image slots use real product-screenshot mockups instead of stock photography (no picsum.photos-style placeholders), and every homepage section without a slot in the mockup (pricing, comparison table, demo video, testimonials, guides, sectors accordion, SEO essay) gets appended below the Trial CTA and above the footer rather than deleted.
  - `home.tsx` hero replaced with the mockup's literal `.hero.on-dark`: kicker, 3-line reveal headline in plain white (the app's `gradient-text` tokens are dark-navy-on-dark-navy and unreadable on this background, so gradient emphasis was dropped here, unlike other pages), sub, 2 actions, note. The real interactive AI input bar had no slot in the locked hero, so it moved into a new "Try it yourself" section in the appended zone (right above the demo video); the animated 3D quote-doc scene was kept as the hero's `.hero-media` (a real screenshot, not a photo).
  - `.stories` trimmed back to exactly the mockup's 3 splits (`#story-quotes`, `#story-crm`, `#story-invoicing`). The 4th split added in Phase 48 (WhatsApp/Integrations, using `WhatsAppChatDemo`) was moved into its own section in the appended zone, reusing the same `.stories`/`.split` classes outside the primary 3-split structure.
  - Trial CTA moved from the very end of the page to immediately after Newsroom (its locked mockup position); everything that used to sit between Newsroom and Trial (pricing, comparison table, testimonials, guides, sectors accordion, SEO essay) now sits after Trial instead, plus the relocated "Try it" bar + demo video + WhatsApp section.
  - **Going forward:** when SEO/city/sector pages get their own redesign pass, reuse these exact locked classes (`.hero`, `.tile`, `.split`, `.impact`, `.news-card`, `.trial`, `.footer`, `.btn*`, `.eyebrow`/`.sec-title`/`.sec-sub`, `.cta-link`) rather than inventing new patterns — that's the point of locking this in.
- **Phase 48c — Homepage cut to exactly the mockup, no appended content. DONE (2026-09-17).** User reviewed 48b live and pushed further: the "append everything below the trial CTA" call from 48b was itself wrong — it still reused old styling for that content and made the page long again. Corrected to: homepage is now **only** hero → product tiles → 3 story splits → impact → newsroom → trial CTA, nothing else. Removed entirely (not moved, not reskinned): the interactive "try it" AI bar, demo video, WhatsApp chat-demo section, pricing plans, comparison table, testimonials, guides grid, sectors/cities accordion, SEO deep-dive essay. That real functionality/content has no home on the page right now — if it needs to live somewhere, it should get its own dedicated page/route in a later phase, not be bolted onto the homepage.
  - Also dropped on the same pass, per "stop using AI-made elements/emojis, this is a professional website": the hero's mouse-parallax 3D tilt and floating badge chips (hero-media is now a plain static card), and the mesh-blob gradient glow behind the trial CTA (plain navy background instead).
  - Fixed a real duplicate-CTA bug found during review: signed-in users saw both a "Dashboard" text link and a "Go to Dashboard" button side by side in the header and mobile drawer. Collapsed to one button in both places.
  - **Open issue, not fixed this pass:** user reported the production site briefly flashing old, unrelated homepage copy ("Get Started Free" / "Forget Excel and handwritten estimates...") on `/dashboard/*` routes before the real page renders. Investigated locally — `scripts/prerender-seo.ts` does not prerender `/dashboard/*` (dashboard chunks are explicitly excluded from SEO prerendering), and `index.html`'s `<div id="root">` ships empty with no static fallback markup, so this isn't reproducible from the dev checkout. Most likely a stale CDN/browser cache serving an old deployment's asset for that route; should self-resolve once this phase's build deploys, but flag for a follow-up check on quoteai.ca after deploy.
- **Phase 48d — No emojis, no AI-mascot/glow/parallax cliches on public pages. DONE (2026-09-17).** Third correction in the same review cycle — user said "no fucking emojis mate, lock this into your memory, no ai elements from now on." Saved as a durable memory ([[feedback-professional-visual-design]]) so this doesn't need repeating. Fixed: `support-bot.tsx`'s floating chat trigger was a cartoon robot SVG with a waving-arm animation — replaced with a plain lucide `MessageSquare` icon, animations removed. Literal emoji replaced with lucide icons in `sign-in.tsx`/`sign-up.tsx` (lock/envelope) and `whatsapp.tsx` (lightning bolt callout, comparison-column labels). **Explicitly left alone:** emoji inside `whatsapp-chat-demo.tsx`'s simulated messages (realistic WhatsApp conversations use emoji) and dashboard/admin-only emoji like "⭐ Most Popular" badges (out of scope — those aren't public marketing pages).
- **Phase 48e — Homepage rebuilt against the new v2 mockup, exactly. DONE (2026-09-17).** User supplied a new, richer mockup and said "this is what i would like the website to be like, exactly like this mate!!" — saved permanently as `docs/mockups/homepage-mockup-v2.html` (now the homepage source of truth, see above) and rebuilt every piece it touches to match 1:1: `mockup-system.css`'s whole homepage section replaced with the v2 classes verbatim, `public-layout.tsx` header/footer rebuilt to the new flat nav + 6-column footer, `home.tsx` fully rebuilt (doc-mock hero, 6 tiles, 3 splits, embedded WhatsApp section, comparison table, impact, newsroom, reviews, guides, trades marquee, deep-dive, CTA), `stats-bar.tsx` impact numbers changed to the new set (still real/live-counted), `testimonials-section.tsx` rebuilt to `.rev-card` markup with the same real TESTIMONIALS data.
  - One deliberate deviation from the mockup, not a design change: the mockup's own footer/marquee trade links all point to the same `#trades` anchor (a placeholder). Wired them to the real `/quotes/:slug/` pages instead — same visual, real navigation. Also dropped the old real `TradesMegaMenu` dropdown (the v2 mockup's "Trades" nav item is a plain anchor to the page's own `#trades` section, not a mega) — individual trade pages are still reachable via the footer and the new trades marquee.
  - Verified live in the Browser pane end to end: hero, all 6 tiles, all 3 story splits (including the reversed one), the WhatsApp section, the comparison table's highlighted quoteai column, impact counters landing on the real values (30 sec / 4–6 hrs / 18 / 5.0), newsroom with real blog posts, reviews with the real RBA/Abdul testimonials, guides, the CTA band, and the Products mega menu opening centered under its trigger. No console errors beyond the pre-existing backend-unreachable ones.
- **Phase 49 — Cleanup pass. DONE (2026-09-17).** Removed dead Phase 30-37 CSS from `index.css` (gradient-border, pro-pulse, quote-row, logo-glow, hero-3d-stage/hero-doc/hero-float-chip/hero-grid-bg, ai-bar-glow, hero-sheen, crm-nav-active/bar-grow-in, mega-menu-panel, the mesh-blob block, plus their keyframes and stale `prefers-reduced-motion` references) — all confirmed zero-usage via grep; kept tokens/classes still genuinely used by not-yet-migrated pages (blog, SEO, sign-in/up, admin). Deleted 33 of 56 `components/ui/*.tsx` shadcn files with zero real imports (accordion, alert, aspect-ratio, avatar, breadcrumb, button-group, calendar, carousel, chart, checkbox, collapsible, context-menu, drawer, empty, field, hover-card, input-group, input-otp, item, kbd, menubar, navigation-menu, pagination, progress, radio-group, resizable, scroll-area, separator, sheet, sidebar, slider, sonner, spinner, table, tabs, toggle, toggle-group) — 19 remain, all in active use. Pruned dead cva variants: `button.tsx`'s `link` variant and `lg` size, `badge.tsx`'s `destructive` variant. Fixed old `bg-card rounded-2xl border border-border shadow-sm` Tailwind soup to `.card` in `pages/dashboard/new.tsx` and `manual-quote-builder.tsx`. No stray `dark:` variants remained. `pnpm run typecheck` clean. `docs/DESIGN-SYSTEM.md` fully rewritten to describe the shipped mockup-system.css approach instead of the superseded Phase 30-37 one.

## Site-wide rollout plan (Phases 50+) — proposed, not started

The homepage (Phases 38-48d) established the locked design system: `.hero`, `.tile`, `.split`, `.impact`, `.news-card`, `.trial`, `.footer`, `.btn*`, `.eyebrow`/`.sec-title`/`.sec-sub`, `.cta-link`, the navy/green/purple/teal/yellow palette, Figtree type. The header/footer (`PublicLayout`) already apply to every public route, including `chi-siamo.tsx`/`contatti.tsx`/`privacy-policy.tsx`/`terms.tsx`/`mappa-sito.tsx` which self-wrap in `<PublicLayout>` — so those already get the locked nav/footer today, only their body content is unstyled. Proposed phase breakdown for the rest of the public site, each its own conversation per convention:

- **Phase 50 — Auth pages. DONE (2026-09-17).** `sign-in.tsx` and `sign-up.tsx` rebuilt onto a new small `.auth-*` vocabulary added to `mockup-system.css` (`.auth-shell`/`.auth-card`/`.auth-card-body`/`.auth-card-foot`/`.auth-title`/`.auth-sub`/`.auth-field`/`.auth-error`/`.auth-link`/`.auth-fine`/`.auth-spin`), built on the same locked tokens (`--navy`, `--line`, `--soft`, `--radius-mk`, `--shadow-card`) and the existing `.btn`/`.btn-navy` pill button — deliberately not the dashboard's `.card`/`.form-grid`/`.field` since these pages render inside `PublicLayout` next to the locked header. Replaced the old ad-hoc `bg-white rounded-3xl border shadow-lg` card, `btn-gradient` button, and gray-bordered Tailwind inputs on both pages (all sign-in modes: password, 2FA, forgot-password/reset-sent; both sign-up states: form and beta-closed). `onboarding.tsx` was left as-is — it doesn't render inside `PublicLayout` (has its own header, no locked nav), and already used the dashboard's `.card`/`.form-grid`/`.field` vocabulary intentionally as a real multi-step business-profile form, which is a reasonable fit for that content. Verified live in the Browser pane (sign-in and sign-up need no auth): both card states render correctly with the locked header/footer.
- **Phase 51 — WhatsApp feature page. DONE (2026-09-17).** `whatsapp.tsx` rebuilt onto the locked classes, keeping all its real content: `.hero.on-dark` (centered, single-column variant — no `.hero-grid`) for the header, a `.split`+`.steps3` "how it works" section reusing the exact vocabulary from the homepage's own WhatsApp teaser (with the real `WhatsAppChatDemo` component as the `.split-media`), a `.card` coming-soon callout, a `.tiles` 3-tile feature grid (`t-green`/`t-purple`/`t-teal`) for the feature list, a `.sec.impact.on-dark` "market reality" band with two `.card` comparison lists (with/without quoteai) — a pattern with no literal 1:1 mockup section, extended from the same card/chip vocabulary per the Phase 45/46 precedent — and closes on the same `.cta.on-dark`/`.cta-bg`/`.cta-in` trial band used on the homepage. Dropped the old purple/blue gradient CTA buttons and gradient text in favor of the locked `.btn-white`/`.btn-outline-light`/`.btn-navy`. All existing `whatsapp.*` i18n keys (EN+FR) reused as-is, no copy changes. `pnpm run typecheck` clean. Verified via live DOM/computed-style inspection in the Browser pane (2-column `.split` grid, correct tile background colors, `.steps3` correctly collapsing to 1 column under its 1080px breakpoint matching the homepage's identical behavior) — the pane's screenshot capture itself was unreliable this session (intermittent blank/mis-scaled captures not matching the verified DOM state), so visual confirmation relied on `getBoundingClientRect`/`getComputedStyle` checks instead of screenshots.
- **Phase 52 — Blog. DONE (2026-09-17).** `blog/index.tsx`, `blog/[slug].tsx`, and `blog/categoria/[slug].tsx` rebuilt onto the locked classes. Index/category listings reuse the homepage's `.news-grid`/`.news-card` verbatim for the article grid, with categories/article tags rendered as `.chip` pills (the 6 blog categories mapped 1:1 onto the 6 existing chip colors: Trades→grey, Pricing→teal, Advice→yellow, Tools→green, Innovation→purple, Business→red — no new chip colors needed). Both list pages open with a centered `.hero.on-dark` band (same single-column variant as `whatsapp.tsx`) and close on the shared `.cta.on-dark`/`.cta-bg`/`.cta-in` trial band. The practical-guides tiles replaced their old emoji icons (`📋`/`🎁`/`📊`/`📄`) with lucide icons in a new `.guide-icon` chip, per the no-emojis-on-public-pages rule. The article page keeps its real content (TOC, related-sector links, related-city links, related articles) but drops the old ad-hoc Tailwind gray/gradient styling for a plain `.wrap`-width header (chip + reading time + navy heading, no full-bleed hero — a long-form article doesn't need one), a new `.blog-toc` card for the TOC, `.blog-link-pill` for the related-sector/city link lists, and a `.blog-prose` class that overrides Tailwind Typography's CSS custom properties (`--tw-prose-*`) to point at the locked navy/muted-mk/line tokens instead of Tailwind's default gray palette — keeping the `prose` plugin's actual typographic rules (spacing, list markers, etc.) while matching the locked color system. New shared vocabulary added to `mockup-system.css` under "BLOG / ARTICLE PAGES": `.crumbs`/`.crumb-sep`/`.crumb-current` (breadcrumbs, not used by any earlier phase since none of phases 38-51 needed one), `.guide-icon`, `.blog-toc`, `.blog-prose`, `.blog-links`/`.blog-link-pill`. All existing `blog.*` i18n keys (EN+FR) reused as-is, no copy changes. `pnpm run typecheck` clean. Verified live in the Browser pane on all three page types (index, article, category) via `get_page_text` and `getComputedStyle`/`getBoundingClientRect` checks (3-column `.news-grid` at desktop width collapsing to 1 column under the shared 1080px breakpoint, correct per-category chip colors) — screenshot capture was unreliable in this session (as in Phase 51), so visual confirmation relied on DOM/computed-style checks instead of screenshots.
- **Phase 53 — SEO profession & city pages. DONE (2026-09-17).** `seo/[type].tsx` (sector hub, ~26 pages) and `seo/city-landing.tsx` (sector × city, ~15 cities × ~22 localizable sectors) rebuilt onto the locked classes. All data/logic (SECTORS, CITIES, layout-variant ordering, osservatorio, JSON-LD, canonical/hreflang, `getSectorFrContent`/city render-engine helpers) left untouched — only markup/classNames changed.
  - `[type].tsx`: new `.crumbs` breadcrumb; `.hero on-dark` two-column `hero-grid` (text + the existing per-sector `QuotePreviewMockup`, kept since this route's page count is bounded — not the city route); benefits as `.dd-feat`/`.fi` icon cards (green/teal/purple cycled); how-it-works as `.steps3`; use-cases and city links as `.card` grids; the 3 guide-specific blocks (Excel/Word comparison, come-fare steps, free-quote pricing) reskinned onto `.cmp`/`table.cmp` and ad hoc `.card` extensions (no locked pricing-card vocabulary exists, so plan cards extend `.card` inline per the Phase 45/46 precedent); "all cities" list reuses `.blog-links`; insights reuse `.card` (not full `.news-card` — no article images in this data); final CTA on `.cta.on-dark` with a picsum `.cta-bg` image (same pattern as blog/whatsapp).
  - `city-landing.tsx` (the actual highest-page-count route): kept deliberately **lighter** than the sector hub — centered single-column `.hero on-dark` (no two-column grid, no `QuotePreviewMockup`, no photo), and the final CTA is `.cta.on-dark` **without** a `.cta-bg` image (plain navy, unlike every other locked CTA) specifically to avoid a repeated image slot across hundreds of prerendered pages. Observatory stats, nearby-city anchors (`.blog-link-pill`), same-city-other-sectors and related-sectors grids all reskinned onto `.card`/`.dd-feat`/`.fi`. Breadcrumb now uses the shared `.crumbs` class instead of ad hoc Tailwind.
  - `pnpm run typecheck` clean. Verified live in the Browser pane (`/quotes/painter/`, `/quotes/painter/toronto/`, `/quotes/excel-template/` — all need no auth): hero, benefits, steps, FAQ, city grids, comparison table, observatory card, nearby-city pills, and final CTA all confirmed via screenshots: navy hero with green highlight, white `.card` grids, `.cmp` comparison table with the green quoteai column. No console errors beyond the pre-existing backend-unreachable ones.
- **Phase 54 — Legal & company pages. DONE (2026-09-17).** `chi-siamo.tsx`, `contatti.tsx`, `privacy-policy.tsx`, `terms.tsx`, `mappa-sito.tsx` rebuilt onto the locked classes, no new hero bands or tile grids — matches the phase's "lowest design complexity" scope.
  - `chi-siamo.tsx`: plain `.wrap`-width centered header (eyebrow + `.h2` + `.lead`, no dark hero), story/who-we-serve sections alternating `.sec`/`.sec.soft`, the 3 value props reskinned onto `.dd-feat`/`.fi` icon rows (same vocabulary as the SEO sector-hub benefits), CTA closes on plain `.btn-navy`/`.btn-outline-navy` (no dark band).
  - `contatti.tsx`: same plain header pattern, 3 contact channels as `.card`+`.fi`+`.cta-link`, response-time note as a single `.dd-feat` row, FAQ as `.card` list (same pattern as the SEO pages' FAQ), CTA closes on `.btn-navy`. No real contact form exists in the prior version (mailto links only) — none was fabricated; only markup/classNames changed.
  - `privacy-policy.tsx` / `terms.tsx`: adopted the blog article page's exact header pattern — `.crumbs` breadcrumb, plain `.wrap`-width title block (no hero), body wrapped in `.prose.blog-prose` for locked navy/muted-mk typography tokens. All legal content, section numbering, and copy left untouched (these still carry the pre-existing "not reviewed by a Canadian lawyer" TODO).
  - `mappa-sito.tsx`: plain centered header, the 3-column main directory and the cities-by-region block reskinned onto `.card` with `.cta-link`/inline muted-token link styling, region cities directory kept as-is structurally (data/logic untouched).
  - `pnpm run typecheck` clean. Verified live in the Browser pane (`/chi-siamo/`, `/contatti/`, `/privacy-policy/`, `/terms/`, `/mappa-sito/` — all need no auth) via screenshots and `get_page_text`: locked header/footer, navy/green typography, card grids, breadcrumbs, and full body copy all confirmed present and unchanged in wording. No console errors beyond the pre-existing backend-unreachable ones.
- **Phase 55 — Public transactional pages. DONE (2026-09-17).** `pages/p/[id].tsx` (quote view), `pages/i/[token].tsx` (invoice view), `pages/sign/[token].tsx` (e-sign), `pages/t/[token].tsx` (worker time entry), `pages/team-invite/[token].tsx` rebuilt onto the locked tokens — no hero/tile/split sections, matching the phase's "functional document, not a marketing page" scope.
  - New small vocabulary added to `mockup-system.css` under "PUBLIC TRANSACTIONAL / DOCUMENT PAGES": `.doc-shell` (page background), `.doc-head` (sticky header bar), `.doc-banner` + `.ok`/`.warn`/`.danger`/`.info` modifiers (status panels — paid/accepted/signed, voided/declined, pending, generic info — built on the locked green/yellow/red/navy tokens), `.doc-status` (small status pill, reusing the chip color logic) for the worker time-entry list. Also added a genuine gap fix to the base `.btn` rule: `.btn:disabled { opacity: .55; pointer-events: none; }` — no previous phase needed a disabled button since marketing CTAs are never disabled, but these are real forms with pending/invalid states.
  - Content cards reuse `.card` directly (with Tailwind padding utilities, per the interop rule) instead of the old ad hoc `rounded-2xl border-slate-200 bg-white shadow-sm` soup; primary actions reuse `.btn`/`.btn-navy`/`.btn-outline-navy`/`.btn-sm` instead of the shadcn `Button`, and plain `.field`-wrapped `<input>`/`<select>` instead of shadcn `Input` (same precedent as `pages/dashboard/profile.tsx`'s `.field` usage — CSS selector specificity means `.field input` needs a bare `<input>`, not the shadcn component's own utility classes, to win the cascade). Old Tailwind `navy-500/600` (the lighter blue-violet scale) and `slate-*`/`emerald-*`/`rose-*`/`amber-*` status colors replaced with the locked `var(--navy)`/`var(--muted-mk)`/`var(--green*)`/`var(--red*)`/`var(--yellow*)` tokens throughout, inline per the established pattern for values with no Tailwind utility counterpart (same as `contatti.tsx`/`onboarding.tsx`).
  - Secondary/destructive controls that have no locked pill-button equivalent (contract decline confirm, small ghost actions) got inline-styled `.btn`/`.btn-sm` variants using `var(--red)` rather than reinventing a new button class for one-off cases.
  - `pnpm run typecheck` clean. Live-verified in the Browser pane on all 5 routes with placeholder tokens (`/p/`, `/i/`, `/sign/`, `/t/`, `/team-invite/` all render their not-found/error state without a real backend, per the existing verification-note caveat that `/api/*` is unreachable in this environment) — confirmed the locked navy/soft/card/icon colors render correctly for each page's empty state. The authenticated happy-path content (real quote/invoice/contract/time-entry data) could not be exercised live for the same reason as every other backend-dependent phase, so those were reviewed by re-reading the diffs against the locked class definitions instead.

- **Phase 56 — 404 page. DONE (2026-09-17).** `pages/not-found.tsx` — the app's global router catch-all — rebuilt onto `PublicLayout` with the locked classes: `.wrap`-width centered content (icon chip on `var(--soft)`/`var(--navy)`, `.eyebrow` "404", `.h2` heading, `.lead` body), CTA as `.btn.btn-navy` linking home. No new vocabulary needed. Reused the existing `errorBoundary.backHome` i18n key for the button label instead of adding a duplicate. Left `components/error-boundary.tsx` (the top-level React error boundary, a separate component from the 404 route) untouched on purpose — it can't use hooks/context since it may render before the app tree mounts, so it deliberately stays framework-free with its own inline styles, same as before. Also left `pages/admin.tsx` out of scope — it's a 2288-line internal admin dashboard behind `/dashboard/admin`, not a public marketing/document page, so it doesn't fit this rollout's scope (same precedent as the Phase 42 `team.tsx` time-entries tab). `pnpm run typecheck` clean. Verified live in the Browser pane (navigating to an unmatched route) via `get_page_text` and `getComputedStyle` — screenshot capture was unreliable again this session (as in Phases 51-53), so confirmed via DOM checks: navy heading/button, locked header/footer both present.

This closes out the Phases 50+ site-wide rollout — every public-facing page and the 404 fallback are now on the locked pixel design system.

## Dashboard detail pages (Phases 57+) — the gap the Phase 41-43 list-page passes left behind

Phases 41-43 rebuilt the dashboard **list** views (quotes/clients/leads, contracts/jobs/team, invoices/analytics/catalog) but never touched the **record** views they link into. A survey on 2026-09-17 found `clients/[name].tsx`, `jobs/[id].tsx` (+ its tab panels in `components/jobs/`), `contracts/[id].tsx`, `invoices/[id].tsx`, `quotes/[id].tsx` (2420 lines) and `new.tsx` (763 lines) all still on shadcn `Card`/`Button`/`Badge` + the old `navy-500`/`slate-*`/`rounded-2xl` Tailwind soup.

- **Phase 57 — Client, job, contract & invoice detail pages. DONE (2026-09-18).** `clients/[name].tsx`, `jobs/[id].tsx`, `contracts/[id].tsx`, `invoices/[id].tsx` plus everything that renders inline inside the job page's tabs — `components/jobs/costs-tab.tsx`, `invoices-tab.tsx`, `team-tab.tsx`, `photos-tab.tsx`, `overview-charts.tsx` (health tiles + slip list; the recharts `ChartCard`s were already on `.card` since Phase 43), `receipt-queue.tsx` (jobs list inbox), and the shared `components/jobs/badges.tsx` + `pages/dashboard/invoices.tsx`'s exported `InvoiceRow` (only the job invoices tab uses it). All data/mutation logic untouched — markup/classNames only.
  - The dashboard mockup has no detail views, so these reuse the list-page vocabulary directly (`.page-head`/`.head-actions`, `.card`/`.card-head`/`.card-foot`/`.act-body`, `.stat-grid`/`.stat-card`, `.chip-*`, `.pills`/`.pill.on` for the job tabs — same as settings — `.q-row` for the client's quote list, `.dropzone` for receipts/photos, `.form-grid`/`.field`/`.set-row`/`MockupToggle` for the contract and invoice draft editors, `.hbar` for budget-vs-actual, `.avat` for assignees, `.btn-sm` navy/outline pills for every action) and add one small "DASHBOARD DETAIL PAGES" section to `mockup-system.css` for what a record page needs and no list page had: `.back-link`, `.page-head .title-row`/`.meta`, `.stat-grid.cols-5` + `.stat-card .sub`/`.val.ok|warn|bad|teal`/`.pbar`, `.notice` (+ `.ok/.warn/.danger/.info/.teal`, the dashboard-side sibling of Phase 55's centered `.doc-banner`), `.kv` key/value rows, `.tl-row`/`.tl-dot` activity timeline, `.item-row` (generic list row: `.ic`/`.date`/`.grow`/`.ttl`/`.sub`/`.amt`/`.hover-act`), `.card-empty`/`.card-sum`/`.card.dashed`, `.ic-btn` (+ `.ok/.bad/.danger`), `.text-link`, `.ms-num`/`.ms-body`/`.card.ms-on|ms-done` milestones, `.chk`/`.task-row` tasks, `.steps-inline`/`.step-pill` contract progress, `.pill .cnt` tab counts, `.dropzone.flush|.on`/`.dz-ic`, `.photo*` gallery tiles, `.doc-view` document frame, `.field.inline`/`.field-hint`/`.form-grid.tight`.
  - `badges.tsx` (job / milestone / change-order / invoice status + type) now emits plain `.chip.chip-*` spans instead of shadcn `Badge` — this flipped the jobs list, invoices list and analytics onto the locked chips too. Same colour logic as the list pages: green = done/paid, teal = in progress/sent, yellow = waiting on someone, red = problem, purple = planned/viewed, grey = draft/closed. `contracts/[id].tsx`'s exported `STATUS_STYLES`/`ContractStatusBadge` (also used by `quote-contract-card.tsx`) got the same treatment; `team-tab.tsx`'s `TimeStatusBadge` (also used by the team page's time tab) too.
  - shadcn `Dialog`s (send / void / sign / record-payment / credit-note / new-invoice / change-order / cost-entry) and the `Button`/`Input`/`Label`/`Textarea` **inside** them were deliberately left on shadcn, matching the Phase 42/43 precedent (`jobs/index.tsx`'s `CreateJobDialog`, `contracts/index.tsx`) — the locked system has no modal vocabulary and inventing one wasn't in scope. Secondary/destructive page actions with no locked equivalent (void, delete draft, archive) use `.btn-outline-navy` with an inline `var(--red)` override or `.text-link`, same as Phase 55.
  - Two small real fixes on the way: the milestone accordion header in `jobs/[id].tsx` was a `<button>` containing more `<button>`s (invalid nesting, React warns) — it's now a `role="button"` div with keyboard handling; and the photo tile's select checkbox was a non-interactive `div` with an `onClick` — now a real `<button aria-pressed>`.
  - `pnpm run typecheck` clean, no unused imports. Live check limited to what this environment allows (see verification note): dev server up, `/dashboard/jobs/:id` redirects to `/sign-in` because `/api/*` is unreachable, and `getComputedStyle` on probe elements confirmed the new section compiles and resolves to the locked tokens (`.notice.ok` → `--green-t`/`--green-dark`, `.stat-card .val.bad` → `--red`, `.card` → 18px radius). No console errors beyond the pre-existing proxy ECONNREFUSED ones.
- **Phase 58 — Quote editor (built 2026-09-18).** The last dashboard surface on shadcn/old Tailwind: `pages/dashboard/new.tsx` (AI composer / manual builder / embedded price catalog tabs) and `pages/dashboard/quotes/[id].tsx` (quote document preview + inline editor + sidebar), plus the components only they use (`manual-quote-builder.tsx`, `price-catalog-section.tsx`, `quote-contract-card.tsx`, `payment-schedule-card.tsx`, `payment-schedule-editor.tsx`, `mic-button.tsx`).
  - New "QUOTE EDITOR (Phase 58)" section in `mockup-system.css`: attachment strips (`.att-strip/.att-thumb/.att-doc/.att-add`), composer extras (`.comp-mic.rec`, `.comp-lock`, `.comp-hint`, `.comp-ex`), `.src.sm`/`.src-grid.flush`/`.src-list` template tiles, `.pick-row` client chips, `.add-dashed`, `.act-list` (full-width sidebar buttons), `.var-block`, the `.paper` document vocabulary for the quote preview (`.paper-head/.paper-title/.paper-sec/.paper-box/.paper-totals/.paper-foot`, `table.ptbl` with navy or `.soft` heads, `tr.chap`/`tr.sum`), `.inl` borderless-until-hover inputs and `.inp-sm` small bordered inputs, `.edit-bar`/`.tpl-bar` banners, `.chap-block/.chap-head/.chap-foot` (+ `.chap-mobile` stacked cards at 640px and below), the manual builder's `.li-head/.li-row/.li-body/.sugg` line grid, `.tax-row`, `.term-list`, `.csv-row/.csv-preview`, `.cat-group`, `.lbl-xs`. Also `.pill` is now `inline-flex` (fixes icons stacking above the label) and `.toolbar .search` stays visible on phones.
  - `new.tsx`: tab switcher is `.pills`, template picker is `.src-grid.flush` + `.src.sm`, the AI bar is the dashboard-home `.composer`/`.comp-row` (same `MicButton`, now emitting `.comp-mic`), example prompts are `.pill`s, the client selector is a `.card` with `.pick-row` chips + `.form-grid.tight`. `ManualQuoteBuilder` and `PriceCatalogSection` rebuilt onto `.card/.card-head/.chap-block/.li-*/.kv/.term-list` and `.toolbar/.search/.item-row/.cat-group` respectively.
  - `quotes/[id].tsx`: `.back-link` + `.page-head` (chips for lock/accepted/pro-spec/template) + `.head-actions` with `.btn btn-sm`; the preview is `.card > .doc-view.paper` with `.edit-bar` sticky on top in edit mode. The three template editors share one `renderEditVoceRow` and the two pro views share `renderProDesc`, so the file went from 2420 to ~1850 lines with identical behaviour. Sidebar cards are `.card` + `.act-list`/`.act-body`/`.src-list`/`.kv-list`.
  - Behaviour fix found on the way: the old preview put `pointer-events-none` on the whole document in view mode, which also disabled the chapter collapse toggles, the attachment links, the client edit hover button and the "regenerate pro description" buttons. `.paper.readonly` now only sets `user-select: none`, so those controls work.
  - Text cleanups: the paperclip emoji in the photo hint and the literal "+" in the add-client labels are gone (icons carry that now); `MicButton` had hardcoded Italian strings, now `mic.dictate/mic.stop/mic.errorTitle` in EN/FR.
  - The four shadcn `Dialog`s on the quote page (pro-spec upgrade, AI regen, paywall, email) and the catalog's form/confirm dialogs stay on shadcn, per the Phase 57 rule.
  - Verification: `pnpm run typecheck` clean. Backend is still unreachable here, so both pages were checked in the Browser pane through a throwaway route that seeds react-query with a fake quote/profile/subscription (deleted before commit): new-quote AI/manual/catalog tabs, quote view + edit mode for standard and Professional templates, and the 375px mobile layout (no horizontal overflow, stacked line-item cards). `pages/admin.tsx` remains out of scope. **With this, Phases 38-58 are complete.** (Phase 59 below picked up two surfaces this survey missed.)

- **Phase 59 — Job setup review page, team time-approval tab, Gantt (built 2026-09-18).** A survey after Phase 58 (grep for shadcn `Card`/`Button`/`Badge` imports + `slate-`/`navy-500`/`rounded-2xl` soup) found two dashboard surfaces every earlier pass had skipped: `pages/dashboard/jobs/setup.tsx` (the AI schedule/budget review page a signed contract lands on — never mentioned in any phase, still 100% old Tailwind + shadcn `Button`/`Input`/`Label`) and the parts of `pages/dashboard/team.tsx` Phase 42 explicitly left as a "light touch" (the whole time-entries tab, plus the shadcn `Button`s and ad hoc `h-8 w-8 rounded-md` icon buttons in the workers/members/equipment row actions). `components/jobs/gantt.tsx`, shared by the setup page and the job detail page, was also still on Tailwind utilities (`bg-blue-500`, `border-rose-400`, `text-slate-*`).
  - New "JOB SETUP + TEAM TIME QUEUE + GANTT (Phase 59)" section in `mockup-system.css`: `.stat-card.editable`/`.val.sm`/`.span-2` (the job-name input card in the summary strip), `.ms-edit` editable milestone row grid (order buttons + title/sub + two dates + payment select + delete; collapses to a 3-column stack at 980px and stacks the dates at 640px via `.dates` switching from `display: contents` to flex), `.shift-start`, `.cov-list` payment-coverage strip (green when linked, yellow when not), `.budget-grid`/`.cell`/`.money-in` ($-prefixed `.inp-sm`), `.card-head .totals`/`.hint`, `.save-bar` (sticky bottom action bar), `.bulk-row`, `.te-group` (per-worker group header inside a single `.card`), `.item-row` extras (`.spacer`/`.flag`/`.by`/`.dim`), `.tbl .row-act`, and the whole `.gantt*` vocabulary (`.gantt-in/.gantt-head/.gantt-lbl/.gantt-axis/.gantt-row/.gantt-track/.gantt-bar.planned|in_progress|completed|skipped|empty/.gantt-today/.gantt-none`) on the locked purple/teal/green/grey/red tokens.
  - `setup.tsx`: `.back-link` + `.page-head`/`.title-row`/`.head-actions` (regenerate + save as `.btn-sm btn-outline-navy`), summary strip as `.stat-grid` of `.card.stat-card`s, the AI-proposal banner as `.notice.info`, schedule card as `.card` + `.card-head` (shift-start date in the head) + Gantt in `.act-body` + `.ms-list` of `.ms-edit` rows + `.add-dashed` + `.cov-list`, budget card with `.budget-grid`, and the sticky `.save-bar` with the green confirm button. All state/mutation logic (draft sync, shift-all, reorder, term linking, margin math) untouched.
  - `team.tsx`: tab pills use the locked `.pill .cnt` count instead of an amber Tailwind badge; every row action is now `.ic-btn`/`.ic-btn.danger`/`.btn-sm btn-outline-navy`/`.text-link` inside `.row-act`; toolbar intros are `.foot-note`; empty states are `.card-empty`. `TimeTab` rebuilt: filter toolbar with `.inp-sm` selects/dates and the CSV export as `.text-link`, bulk-approve `.bulk-row`, entries grouped per worker inside one `.card` with `.te-group` headers and `.item-row`s (`.chk` select button, `.date`, project link + hours + milestone, geofence `.flag`, `TimeStatusBadge`, `.amt`, approve/reject as `.ic-btn.ok/.bad`, reopen/delete on hover via `.hover-act`), `.card-sum` total. The five shadcn `Dialog`s (worker, invite link, member invite, member link, equipment) and the controls inside them stay on shadcn, per the Phase 57 rule.
  - Real fix found on the way: the mockup's own `.card + .card { margin-top: 16px }` sibling rule leaks into grid cells, so cards 2..n of every `.stat-grid` (dashboard home, job detail KPIs, this page) sat 16px lower than card 1 — the mockup HTML has the same defect. Added `.stat-grid > .card, .qa-grid > .card, .split-2 > .card, .mid-grid > .card, .steps-3 > .card, .kanban > .card { margin-top: 0 }` (grids space with `gap`).
  - Verification: `pnpm run typecheck` clean. Both pages checked in the Browser pane through the usual throwaway route seeding react-query (fake job with contract terms/milestones/budget, fake workers + submitted time entries; deleted before commit). This session's Browser pane was only 320px wide, so screenshots weren't usable — verified with `getComputedStyle`/`getBoundingClientRect` probes instead: at 1280px the milestone grid resolves to `22px 1.3fr 150px 150px 1fr 30px`, budget to 3 columns, Gantt renders 4 status-coloured bars + today marker with the 224px label column, save bar is `position: sticky`; at 375px no horizontal overflow (Gantt scrolls inside its own container), stat cards stack to one column, milestone rows collapse to the 3-column layout with stacked dates. Team page: pill count renders, 6 `.ic-btn`s + 2 outline buttons in the worker rows, time tab shows 2 groups / 3 rows / 3 checkboxes / geofence flag, selecting a row updates "Approve selected (1)". Zero leftover `slate-`/`emerald`/`amber`/`rose-`/`rounded-md` classes outside dialogs. Only console errors are the pre-existing backend-unreachable 500s.
  - `pages/admin.tsx` remains the one dashboard surface deliberately left on shadcn.

Each phase should end with the same verification this pass used: typecheck clean, live Browser-pane check (these routes need no auth), no emoji/mascot/glow-parallax regressions per [[feedback-professional-visual-design]].

## Verification note (carried over from Phases 34-37)

This environment's dev server backend (`/api/*`) is unreachable, so authenticated dashboard pages can't be visually checked live in the Browser pane. The homepage (Phase 48) *can* be checked live since it needs no auth — do a real visual diff against `docs/mockups/homepage-mockup.html` there. Dashboard phases rely on typecheck + careful code review against the mockup's markup.
